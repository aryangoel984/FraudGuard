# Neon PostgreSQL database URL used by Prisma.
# Replace with your own database connection string on Neon/PostgreSQL.
DATABASE_URL="postgresql://username:password@hostname:port/dbname?sslmode=require"
